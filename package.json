{
  "name": "olympus-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ olympus",
    "create-local": "graph create graphprotocol/olympus --node http://127.0.0.1:8020",
    "remove-local": "graph remove --node http://pokt.drondin.xyz:18020/ olympus",
    "deploy-local": "graph deploy graphprotocol/olympus --debug --ipfs http://localhost:5001 --node http://127.0.0.1:8020/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
